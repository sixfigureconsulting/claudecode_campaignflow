# Dev Log — 2026-04-06

## Summary
Integrated a Cialdini-based outreach copywriter framework into Step 4 (Generate Sequences) of the campaign workflow. All 7 principles from "Influence" by Dr. Robert Cialdini are now embedded into every generated sequence, with a user-selectable primary driver. Committed and deployed to Vercel via GitHub push.

---

## Feature Built

### Cialdini Influence Framework — Step 4 Sequence Generator

**Goal:** Replace generic AI-written outbound copy with behaviorally-grounded copy based on Robert Cialdini's 7 Principles of Influence, so sequences feel human and psychologically resonant — not AI-generated.

**How it works:**
- User selects a **primary influence principle** before generating sequences
- The selected principle becomes the lead driver of the copy (e.g., "Open with a specific insight" for Reciprocity, "Micro-ask first" for Commitment)
- All 7 principles are woven into every sequence by the base system prompt
- The selected principle is injected as a `PRIMARY DRIVER` instruction in the per-lead prompt

**The 7 categories available to the user:**

| Principle | Tagline | Behaviour |
|-----------|---------|-----------|
| Reciprocity | Lead with genuine value | Opens with tailored insight, mini-audit, checklist. CTA = receive value, not book call |
| Commitment | Micro-ask first, ladder up | First CTA is "Worth a 2-min skim?" — no 30-min meeting asks upfront |
| Social Proof | Show peer results | Leads with specific peer result — segment, firm type, 1–2 concrete metrics |
| Liking | Genuine research first | Opens with observable research about the prospect (hire, post, product launch) |
| Authority | Establish niche credibility | 1–2 compact cues: niche focus + result pattern. No long bios |
| Scarcity | Honest urgency only | Concrete + explained limits only ("3 build slots this quarter"). No fake urgency |
| Unity | Same tribe framing | Shared identity language: "founder-led", "bootstrapped B2B", in-group framing throughout |

**UI changes (`Step4GenerateSequences.tsx`):**
- New **Influence Framework** section with 7 principle cards in a responsive 2×4 grid
- Each card: icon, label, one-line tagline, unique color scheme (emerald/blue/purple/rose/amber/red/cyan)
- Selected principle shows a contextual hint banner explaining the copy behavior
- Generate button label dynamically reflects selected principle: _"Generate Authority-led sequences for 12 leads"_
- Loading message also reflects the principle

**API changes (`generate-sequences/route.ts`):**
- `BASE_SYSTEM_PROMPT` now contains the full Cialdini framework (all 7 principles, style rules, channel-specific defaults, anti-patterns to avoid)
- `INFLUENCE_INSTRUCTIONS` map: per-principle `PRIMARY DRIVER` instruction injected into each lead's prompt
- Execution log records the principle used
- Temperature bumped from 0.7 → 0.75 for slightly more natural variation

**Validation (`validations/index.ts`):**
- Added `INFLUENCE_TYPES` const array and `InfluenceType` export type
- Added `influenceType` field to `generateSequencesSchema` (defaults to `"reciprocity"`)

**Files changed:**
- `src/components/executions/steps/Step4GenerateSequences.tsx`
- `src/app/api/executions/generate-sequences/route.ts`
- `src/lib/validations/index.ts`

---

## Deployment

- **Commit:** _(see git log)_
- **Message:** `feat: Cialdini influence framework for Step 4 sequence generation`
- **Branch:** `main`
- **Remote:** `https://github.com/sixfigureconsulting/claudecode_campaignflow.git`
- **Vercel:** Auto-deployed on push
- **Supabase migrations:** None — no schema changes

---

## Known Remaining Items (not built today)
- LandingFAQ still references CSV upload and multiple clients (old features)
- Smartlead/HeyReach/Lemlist campaign-level pickers (currently aggregate all)
- Week-over-week comparison view
- Could add a "preview" mode to show the principle in action before generating all leads
